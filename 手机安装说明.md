# 育儿知识库手机版安装说明

## iPhone / iPad

此版本是可添加到主屏幕的 PWA，不需要 Mac，也不需要 App Store。

1. 将整个项目部署到一个 HTTPS 网站。可使用 GitHub Pages、Cloudflare Pages 或 Netlify。
2. 用 Safari 打开部署后的网址。
3. 点击 Safari 底部的“分享”按钮。
4. 选择“添加到主屏幕”。
5. 点击主屏幕上的“育儿知识”图标进入。

注意：直接在“文件”App 中打开 index.html 只能预览，不能完成离线安装；必须通过 HTTPS 网站打开。

## 导入新知识库

1. 在 ChatGPT 中下载新版 JSON 知识库更新包。
2. 将 JSON 保存到 iPhone 的“文件”App。
3. 打开育儿知识库 App。
4. 进入“更新”页面，点击“导入知识更新包”。
5. 在文件选择器中选择下载的 JSON。

导入后无需重新安装 App。

## Android

1. 用 Chrome 打开部署网址。
2. 点击浏览器菜单。
3. 选择“安装应用”或“添加到主屏幕”。

## 电脑本地预览

不要直接双击 index.html。请在项目目录运行：

```bash
python3 -m http.server 8080
```

然后打开 `http://localhost:8080`。
